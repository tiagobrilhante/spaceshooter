// pontuacao mais alta

randomize();

global.max_pontos = 0;

global.paused = false;

// num morte
global.total_mortes = 0;

// num inimigos mortos
global.total_inimigos = 0;


inicia_musica_inicial();


room_goto(rm_inicio);
